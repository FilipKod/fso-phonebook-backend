{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../fso-phonebook-frontend && pnpm run build && cp -r dist ../fso-phonebook-backend",
    "deploy:full": "pnpm run build:ui && git add . && git commit -m 'ui build' && git push"
  },
  "author": "Filip Madunicky",
  "license": "MIT",
  "packageManager": "pnpm@10.15.0",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
